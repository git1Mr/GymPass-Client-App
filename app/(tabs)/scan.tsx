// Placeholder route backing the tab bar's center slot. The slot's
// tabBarButton renders the AccessAnyGym FAB (which opens the QR check-in
// modal instead of navigating), so this screen is never shown.
export default function ScanPlaceholder(): null {
  return null;
}
