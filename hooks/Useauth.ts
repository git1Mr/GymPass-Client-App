// hooks/useAuth.ts
// Re-exports useAuth from context so screens follow the same
// hooks/ import convention used throughout the Expo default template.

export { useAuth } from "@/context/AuthContext";

